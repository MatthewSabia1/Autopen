// This component is not needed for the Textera UI as it doesn't have a sidebar
// We'll keep an empty component for now to avoid breaking imports

import React from "react";

const Sidebar = () => {
  return null;
};

export default Sidebar;
